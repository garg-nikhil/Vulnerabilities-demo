<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>demo</groupId>
    <artifactId>vuln-app</artifactId>
    <version>1.0</version>

    <dependencies>

        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
            <version>3.2.1</version>
        </dependency>

        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>1.2.17</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>4.3.0.RELEASE</version>
        </dependency>

    </dependencies>
</project>

/* SecurePilot patch (manual merge needed) */
// Upgrade commons-collections:commons-collections to the latest secure version

/* SecurePilot patch (manual merge needed) */
// Upgrade log4j:log4j to the latest secure version

/* SecurePilot patch (manual merge needed) */
// Upgrade org.springframework:spring-core to the latest secure version
