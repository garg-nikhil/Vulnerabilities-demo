module demo

go 1.18

require (
    github.com/gin-gonic/gin v1.6.3
    golang.org/x/text v0.3.2
)

/* SecurePilot patch (manual merge needed) */
// Upgrade github.com/gin-gonic/gin to the latest secure version

/* SecurePilot patch (manual merge needed) */
// Upgrade golang.org/x/text to the latest secure version
