# ============================
# Deliberately Insecure Dockerfile
# For SAST/IaC Scanner Testing Only
# ============================

# 1. Latest tag used (unpinned image)
FROM ubuntu:latest

# 2. Maintainer deprecated
MAINTAINER admin@example.com

# 3. Hardcoded secrets
ENV DB_PASSWORD=Password123
ENV AWS_SECRET_ACCESS_KEY=abc123456789secret
ENV API_KEY=AIzaSyDummyAPIKey

# 4. Install everything without version pinning
RUN apt-get update && \
    apt-get install -y \
        curl \
        wget \
        vim \
        git \
        python3 \
        openssh-server \
        net-tools \
        telnet \
        ftp \
        sudo

# 5. Download and execute remote script
RUN curl -fsSL https://example.com/install.sh | bash

# 6. Disable SSL verification
RUN git config --global http.sslVerify false

# 7. Download binary without checksum verification
RUN wget https://example.com/myapp.tar.gz && \
    tar -xzf myapp.tar.gz

# 8. Copy SSH private key into image
COPY id_rsa /root/.ssh/id_rsa

# 9. Copy .env containing secrets
# ============================
# Deliberately Insecure Dockerfile
# For SAST/IaC Scanner Testing Only
# ============================

# 1. Latest tag used (unpinned image)
FROM ubuntu:latest

# 2. Maintainer deprecated
MAINTAINER admin@example.com

# 3. Hardcoded secrets
ENV DB_PASSWORD=Password123
ENV AWS_SECRET_ACCESS_KEY=abc123456789secret
ENV API_KEY=AIzaSyDummyAPIKey

# 4. Install everything without version pinning
RUN apt-get update && \
    apt-get install -y \
        curl \
        wget \
        vim \
        git \
        python3 \
        openssh-server \
        net-tools \
        telnet \
        ftp \
        sudo

# 5. Download and execute remote script
RUN curl -fsSL https://example.com/install.sh | bash

# 6. Disable SSL verification
RUN git config --global http.sslVerify false

# 7. Download binary without checksum verification
RUN wget https://example.com/myapp.tar.gz && \
    tar -xzf myapp.tar.gz

# 8. Copy SSH private key into image
COPY id_rsa /root/.ssh/id_rsa

# 9. Copy .env containing secrets
# 1. Pinned base image version
FROM ubuntu:22.04

# 2. Maintainer specified via LABEL
LABEL maintainer="admin@example.com"

# 3. Create non-root user and directory with safe permissions
RUN groupadd -r appgroup && useradd -r -g appgroup -d /app -s /sbin/nologin appuser && \
    mkdir -p /app && \
    chown -R appuser:appgroup /app

WORKDIR /app

# 4. Minimal package installation with APT cache cleanup
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        python3 \
        ca-certificates && \
    rm -rf /var/lib/apt/lists/*

# 5. Copy required files with non-root ownership
COPY --chown=appuser:appgroup app.py startup.sh /app/

# 6. Set appropriate file permissions
RUN chmod 755 /app/startup.sh

# 7. Run container as non-root user
USER appuser

# 8. Execute single process via CMD array format
CMD ["python3", "app.py"]

# 10. World-writable permissions
RUN chmod -R 777 /app

# 11. Run everything as root
USER root

# 12. Expose unnecessary ports
EXPOSE 22
EXPOSE 3306
EXPOSE 6379

# 13. Create privileged sudo user
RUN useradd hacker && \
    echo "hacker:hacker123" | chpasswd && \
    usermod -aG sudo hacker

# 14. Enable password login for SSH
RUN echo "PermitRootLogin yes" >> /etc/ssh/sshd_config && \
    echo "PasswordAuthentication yes" >> /etc/ssh/sshd_config

# 15. Insecure file permissions
RUN touch /etc/secret.conf && \
    chmod 666 /etc/secret.conf

# 16. Add entire repository (including secrets)
ADD . /app

WORKDIR /app

# 17. Run custom startup script
RUN chmod +x startup.sh

# 18. Run multiple services
CMD service ssh start && \
    python3 app.py


# 10. World-writable permissions
RUN chmod -R 777 /app

# 11. Run everything as root
USER root

# 12. Expose unnecessary ports
EXPOSE 22
EXPOSE 3306
EXPOSE 6379

# 13. Create privileged sudo user
RUN useradd hacker && \
    echo "hacker:hacker123" | chpasswd && \
    usermod -aG sudo hacker

# 14. Enable password login for SSH
RUN echo "PermitRootLogin yes" >> /etc/ssh/sshd_config && \
    echo "PasswordAuthentication yes" >> /etc/ssh/sshd_config

# 15. Insecure file permissions
RUN touch /etc/secret.conf && \
    chmod 666 /etc/secret.conf

# 16. Add entire repository (including secrets)
ADD . /app

WORKDIR /app

# 17. Run custom startup script
RUN chmod +x startup.sh

# 18. Run multiple services
CMD service ssh start && \
    python3 app.py
