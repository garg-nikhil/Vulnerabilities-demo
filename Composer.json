{
    "require": {
        "symfony/http-foundation": "2.7.0",
        "twig/twig": "1.20.0",
        "monolog/monolog": "1.17.0"
    }
}

/* SecurePilot patch (manual merge needed) */
// Upgrade symfony/http-foundation to the latest secure version

/* SecurePilot patch (manual merge needed) */
// Upgrade twig/twig to the latest secure version
